# Copy this file to ".env" and fill in your backend URL.
# In local development you can leave this blank (the Vite proxy handles it).
# In production (Vercel), set VITE_API_URL to your deployed Render backend URL,
# e.g. https://research-radar-api.onrender.com
VITE_API_URL=
